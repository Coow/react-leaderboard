import { DiscordLoginButton } from "react-social-login-buttons";

//<DiscordLoginButton className="max-w-xs" onClick={() => alert("Hello")} />

import React from "react"

export default function() {
    return(
        <div></div>
    )
}